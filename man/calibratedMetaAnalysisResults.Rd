% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MetaAnalysis.R
\name{calibratedMetaAnalysisResults}
\alias{calibratedMetaAnalysisResults}
\title{Compute and Upload Calibrated Meta-Analysis Results for Dashboard}
\usage{
calibratedMetaAnalysisResults(config = config::get(), dashboard)
}
\arguments{
\item{config}{List. Project configuration, typically from `config::get()`, containing compute settings, connection details, and dashboard paths.}

\item{dashboard}{Dashboard configuration object with results schema, type, and cohort mapping.}
}
\value{
Invisible `NULL`. Used for its side effects (file creation and database upload).
}
\description{
Performs meta-analysis on SCC results for the given dashboard, calibrates the results using negative controls, and uploads the final calibrated results to the dashboard's results schema in the database.
}
\details{
- Sets up parallel processing according to configuration.
- Loads SCC results for all cohort-outcome/analysis combinations from the database.
- Performs meta-analysis grouping by targets/outcomes and computes summary statistics.
- Loads negative control data, aligning each with corresponding analysis groups.
- Calibrates meta-analytic results using negative controls via `SelfControlledCohort:::computeCalibratedRows()` for each group.
- Exports the calibrated results locally and uploads them to the dashboard database schema.

Results are grouped/calibrated by outcome (for outcome dashboards) or by exposure (for exposure dashboards).
}
\examples{
\dontrun{
calibratedMetaAnalysisResults(config, dashboard)
}

}
\seealso{
[SelfControlledCohort:::computeCalibratedRows()], [SelfControlledCohort::uploadResults()]
}
